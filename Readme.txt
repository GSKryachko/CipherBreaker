This program will try to decrypt text, encrypted with substitution cipher.
Before encoding, you need to build masks. It can be done using build argument,
you need a file with english words.
Use command get_key to get key. It will be saved to txt file.
After that, you can decrypt text.

Also for testing purposes you can use command encrypt, it will use random key.

Run program with -h for more detailed help